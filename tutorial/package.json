{
  "name": "zlink-tutorial",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:http-client": "npm --prefix HttpClient run build",
    "server": "node -e \"require('node:fs').existsSync('dist') || (console.error('run npm run build first'), process.exit(1))\" && node dist/Server/main.js",
    "client": "node -e \"require('node:fs').existsSync('dist') || (console.error('run npm run build first'), process.exit(1))\" && node dist/Client/main.js",
    "http-client": "npm --prefix HttpClient start"
  },
  "dependencies": {
    "@zlink-systems/framework": "0.23.0",
    "@zlink-systems/framework-locations-redis": "0.23.0",
    "@zlink-systems/nestjs": "0.23.0",
    "@nestjs/common": "10.4.22",
    "@nestjs/core": "10.4.22",
    "reflect-metadata": "0.2.2"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "@types/node": "22.15.30"
  }
}
